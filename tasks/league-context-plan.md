# Plan — League context wiring

1. Owner identity helpers (id + name match to current league).
2. Lookback: season picker live badges + owner career board + my-team by id.
3. Trades: enrich with lookback owner grades when ready; re-render on lookback ready.
4. Wire lookback completion → `renderTrades()`; ensure all tabs refresh on connect.
5. Verify script + commit.
